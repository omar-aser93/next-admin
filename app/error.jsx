"use client"

//Client component , to show errors on other server components .. (error.jsx is a default name from next)
const Error = () => {
  return (    
    <div>Error</div>          //The error layout
  )
}

export default Error